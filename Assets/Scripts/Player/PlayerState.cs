namespace Player
{
    /// <summary>
    /// Player'ın state'inin editörden rahatça görünmesi için bir enum sınıfı ileride kaldırılabilir.
    /// </summary>
    public enum PlayerState
    {
        Idle,
        Move,
        Shoot
    }
}